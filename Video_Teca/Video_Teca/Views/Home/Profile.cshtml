@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<form asp-action="EditProfile" method="post">
    <h3>Registration</h3>
    @*
    <div class="form-group">
        <label>Name</label>
        <input type="text" class="form-control" asp-for="Name" />
        <span class="text-danger" asp-validation-for="Name"></span>
    </div>

    <div class="form-group">
        <label>UserName</label>
        <input type="text" class="form-control" asp-for="UserName" />
        <span class="text-danger" asp-validation-for="UserName"></span>
    </div>

    <div class="form-group">
        <label>Email</label>
        <input type="text" class="form-control" asp-for="Email" />
        <span class="text-danger" asp-validation-for="Email"></span>
    </div>

    <div class="form-group">
        <label>Password</label>
        <input type="password" class="form-control" asp-for="Password" />
        <span class="text-danger" asp-validation-for="Password"></span>
    </div>

    <div class="form-group">
        <label>Password Confirm</label>
        <input type="password" class="form-control" asp-for="PasswordConfirm" />
        <span class="text-danger" asp-validation-for="PasswordConfirm"></span>
    </div>*@

    @if (TempData["msg"] != null)
    {
        <div class="form-group">
            @TempData["msg"]
        </div>
    }

    <button type="submit" class="btn btn-primary">SignUp</button>
</form>

<div class="row justify-content-center">
    <img class="img-profile" src="~/images/img-default.webp" />
</div>

<div class="row justify-content-center">
    <input type="file" name="ImageData" id="ImageData" onchange="cambiarImagen(this)"/>
</div>


@section scripts {
    <script type="text/javascript">
        $(document).ready(function () {

            

        });

        function cambiarImagen(input) {  //Cambia la imagen por la seleccionada
            if (input.files && input.files[0]) {
                var leer = new FileReader();

                leer.onload = function (e) {
                    document.getElementsByClassName("img-profile")[0].setAttribute("src", e.target.result);
                    document.getElementsByClassName("img-profile")[1].setAttribute("src", e.target.result);

                    //document.getElementById("img-profile")[0].setAttribute("src", e.target.result);
                }

                leer.readAsDataURL(input.files[0]);
            }

        }

        window.onload = function () {
            var imageUrl = '@ViewBag.ImageUrl';
            localStorage.setItem('ImageUrl', imageUrl);
            console.log(imageUrl.toString());
            console.log("laputamadre");
        };
    </script>
}

